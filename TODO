update to ffmpeg 0.8.3 (fixed crash when opening files)

edit oma.c in ffmpeg and add support for atrac3plus
	use sceAudiocodecDecode for atrac3plus

finish settings //file list ini not reading with minglue-psp.h
	
show cpu and gpu load

after returning to file browser from now playing, cursor will exceed list
		// happens only if you get to now playing by pressing select or from main menu

