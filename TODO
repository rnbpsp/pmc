
compile ffmpeg into a prx and use like a shared library
	waaaaaay faster compilation(link time) of the main app
	only export needed functions
	*malloc problem, needs to be done by main app and not prx

use fatfs for reading "ms0:" using "msstor:"

fix seams between 512px boundary on large images

finish settings //ini not reading with minglue-psp.h

update ffmpeg patch
	dsputil.c
	fmtconvert.c
